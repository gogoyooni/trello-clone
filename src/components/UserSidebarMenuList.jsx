import React from "react";

export default function UserSidebarMenuList() {
  const menuList = [{ name: "Board" }, { name: "Members" }];
  return (
    <ul>
      {menuList?.map((menu) => (
        <></>
      ))}
    </ul>
  );
}
